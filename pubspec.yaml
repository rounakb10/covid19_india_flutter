name: covidindiaflutter
description: A new Flutter application.

publish_to: 'none'
version: 1.1.9+19

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_staggered_animations: ^0.1.2
  provider: ^4.1.2
  shared_preferences: ^0.5.7+3
  http: ^0.12.1
  flutter_spinkit: ^4.1.2
  morpheus: ^1.2.2+1
  liquid_pull_to_refresh: ^2.0.0
  animations: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_automation:

flutter:
  uses-material-design: true